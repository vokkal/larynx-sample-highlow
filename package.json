{
  "name": "larynx-sample-highlow",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "devDependencies": {
    "@types/node": "^7.0.12",
    "source-map-loader": "^0.2.1",
    "ts-loader": "^2.0.3",
    "tslint": "^4.5.1",
    "typescript": "^2.2.2",
    "webpack": "^2.4.1",
    "zip-webpack-plugin": "^1.1.0"
  },
  "dependencies": {
    "larynx-sdk": "^1.0.1"
  },
  "scripts": {
    "build": "tslint -c tslint.json 'src/**/*.ts' && rm -rf ./build && tsc -p . && cp -r ./src/templates ./build/src",
    "webpack": "webpack",
    "pack": "npm run build && rm -rf ./dist && mkdir ./dist && pushd ./build/src && zip -r1q ../../dist/highlow.zip . && popd"
  },
  "author": "",
  "license": "SEE LICENSE IN License.txt"
}
